package _030404_flags_

import (
	"fmt"
)

func Demo() {
	fmt.Println("\n030404 Go Templates: Flags")

	//var port, message string
	//
	//flag.StringVar(&port, "port", "8080", "The HTTP Port")
	//flag.StringVar(&message, "message", "", "The Response Message")
	//flag.Parse()
	//
	//http.HandleFunc("/", func(w http.ResponseWriter, r *http.Request) {
	//	w.Write([]byte(message))
	//})
	//
	//http.ListenAndServe(":"+port, nil)

	fmt.Println("Uncomment lines 10-20 in `flags.go` to demo this chapter.")
}
